# BinGO 教师端（macOS / iPadOS）

这是原生 SwiftUI 多平台教师客户端，不包含网页或 WebView。使用 XcodeGen 生成工程：

```bash
xcodegen generate
open BinGOTeacher.xcodeproj
```

选择 `BinGOTeacher-macOS` 或 `BinGOTeacher-iPad` Target 后配置签名并运行。
