<Window
    x:Class="BinGOTeacher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="BinGO 教师端">
    <Grid x:Name="RootGrid" Background="#FFF4F7FB" Loaded="RootGrid_Loaded">
        <NavigationView x:Name="Navigation" Visibility="Collapsed" IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False" SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="总览" Tag="dashboard" />
                <NavigationViewItem Content="班级与学生" Tag="students" />
                <NavigationViewItem Content="教学任务" Tag="tasks" />
                <NavigationViewItem Content="成果与批改" Tag="submissions" />
                <NavigationViewItem Content="学习小组" Tag="groups" />
                <NavigationViewItem Content="消息" Tag="messages" />
                <NavigationViewItem Content="通知" Tag="notifications" />
                <NavigationViewItem Content="设置" Tag="settings" />
            </NavigationView.MenuItems>
            <Grid>
                <ContentControl x:Name="Workspace" />
                <ProgressRing x:Name="BusyRing" Width="42" Height="42" IsActive="False" Visibility="Collapsed"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </NavigationView>
        <Grid x:Name="LoginView" Background="#FFF4F7FB">
            <Border Width="430" Padding="36" CornerRadius="20" Background="White"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Spacing="14">
                    <TextBlock Text="BinGO 教师端" FontSize="30" FontWeight="SemiBold" />
                    <TextBlock Text="原生 Windows 教师工作台" Foreground="#FF64748B" />
                    <TextBox x:Name="UsernameBox" Header="用户名" />
                    <PasswordBox x:Name="PasswordBox" Header="密码" />
                    <TextBox x:Name="InviteBox" Header="教师邀请码（注册时填写）" />
                    <Button x:Name="LoginButton" Content="登录" Click="LoginButton_Click" />
                    <Button Content="使用邀请码注册教师账号" Click="RegisterButton_Click" />
                    <TextBlock x:Name="LoginStatus" Foreground="#FFB42318" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
        <InfoBar x:Name="StatusBar" IsOpen="False" IsClosable="True" VerticalAlignment="Top" Margin="16" />
    </Grid>
</Window>
